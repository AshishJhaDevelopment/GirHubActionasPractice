name: Reusable caller
on:
  workflow_dispatch: # This means that we will run this manually on UI

jobs:
  initiate_reusable:
    runs-on: ubuntu-latest
    uses: actions/checkout@v2  # Fixed the action name to `actions/checkout`
    defaults:
      run:
        working-directory: .github/  # Default working directory for all run steps
    steps:
      - name: Call reusable workflow
        uses: .github/ReusableWorkflowFIles/reusedWorkflowCalled.yml@V1
        with:
          name_param: Ashish_Jha_Ji
