name: run scripts under workflow
on:
  push:
jobs:
  job1:
    defaults:
      run:
        working-directory: ./workflows/scripts
    runs-on: ubuntu-latest
    steps:
      - name: running scripts
        run: bash scriptexe.sh 